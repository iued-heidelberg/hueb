- name: create the .env file
  template:
    src: ../templates/env.j2
    dest: "{{ src_directory }}/.env"

- name: Change permissions for .env file
  file:
    path: "{{ src_directory }}/.env"
    owner: "{{ user_name }}"
    state: file
    mode: 0700

