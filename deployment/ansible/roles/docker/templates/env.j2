#!/bin/bash
export TAG={{ tag }}
export GIT_SHA={{ git_sha }}

export ENV={{environment}}
export DEBUG={{ debug }}
export STATIC_DIR={{static_directory}}
export ALLOWED_HOSTS={{url}}
export URL={{url}}

export BACKUP_DIR={{backup_dir}}
export NGINX_CONF={{nginx_conf}}
export SSL_DIR={{ssl_dir}}

export HONEYCOMB_API_KEY={{honeycomb_api_key}}
export SENTRY_API_KEY={{sentry_api_key}}
export SECRET_KEY="{{django_secret_key}}"

export DB_NAME={{db_name}}
export DB_USER={{db_user}}
export DB_PASSWORD={{db_password}}
export DB_PORT={{db_port}}
export HOST_PG_PORT={{host_db_port}}



